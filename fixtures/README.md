# Test fixtures

Drop a short portrait clip here as `clip.mov` (or `clip.mp4`).
2–6s, one clear moving subject, uncluttered background.

It gets bundled into the app (simulator-only "Use test clip" button) so the
full capture→pipeline→brush→draw loop can be exercised without a camera.
Video files are gitignored so they do not bloat the repo.
